apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ahla-meet-signaling.fullname" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "ahla-meet-signaling.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
