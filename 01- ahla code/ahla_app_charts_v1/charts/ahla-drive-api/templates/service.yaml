apiVersion: v1
kind: Service
metadata:
  name: {{ include "ahla-drive-api.fullname" . }}
  labels:
{{ include "ahla-drive-api.labels" . | indent 4 }}
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: {{ include "ahla-drive-api.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.service.port }}
