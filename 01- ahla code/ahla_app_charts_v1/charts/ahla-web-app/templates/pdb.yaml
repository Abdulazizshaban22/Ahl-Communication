apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "ahla-web-app.fullname" . }}
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "ahla-web-app.name" . }}
      app.kubernetes.io/instance: {{ .Release.Name }}
