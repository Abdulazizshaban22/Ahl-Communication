# Ahla Meet — Insertable Streams (E2EE) Scaffolding

- Hooks for E2EE via WebRTC Insertable Streams.
- Replace the worker.js with **SFrame**-based encryption for production usage.
- Requires enabling `rtpSender.transform` (supported in modern Chromium-based browsers).
