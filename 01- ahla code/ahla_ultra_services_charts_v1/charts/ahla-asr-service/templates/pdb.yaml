apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: { include "ahla-asr-service.fullname" . }
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: { include "ahla-asr-service.name" . }
      app.kubernetes.io/instance: { .Release.Name }
