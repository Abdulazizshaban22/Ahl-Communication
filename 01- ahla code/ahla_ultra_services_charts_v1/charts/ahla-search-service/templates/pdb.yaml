apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: { include "ahla-search-service.fullname" . }
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: { include "ahla-search-service.name" . }
      app.kubernetes.io/instance: { .Release.Name }
