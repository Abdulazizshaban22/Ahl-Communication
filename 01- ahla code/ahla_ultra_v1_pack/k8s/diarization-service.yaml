apiVersion: apps/v1
kind: Deployment
metadata:
  name: diarization-service
  labels: { app: diarization-service }
spec:
  replicas: 2
  selector: { matchLabels: { app: diarization-service } }
  template:
    metadata: { labels: { app: diarization-service } }
    spec:
      containers:
        - name: app
          image: <REG>/ahla/diarization-service:ultra
          ports: [{ containerPort: 8090 }]
          readinessProbe: { httpGet: { path: /healthz, port: 8090 }, initialDelaySeconds: 5, periodSeconds: 5 }
          livenessProbe:  { httpGet: { path: /healthz, port: 8090 }, initialDelaySeconds: 10, periodSeconds: 10 }
---
apiVersion: v1
kind: Service
metadata:
  name: diarization-service
spec:
  selector: { app: diarization-service }
  ports: [{ name: http, port: 80, targetPort: 8090 }]
