
# CloudWatch Alarms
- CPU > 70% (2 دقائق) لكل خدمة → SNS Email
- Memory > 75% لكل خدمة → SNS Email
- ALB 5xx > 1 في دقيقة → SNS Email

لتفعيل الاشتراك، وافق على رسالة الـSNS في بريدك بعد أول تطبيق لـTerraform.
