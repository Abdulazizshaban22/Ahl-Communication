# Docs & References
- Keycloak OIDC client (confidential) + NextAuth Keycloak Provider.
- NATS JetStream: durable & ephemeral consumers; server supports WebSocket gateway.
- MinIO: Presigned GET/PUT for direct uploads/downloads to private buckets.
- CoTURN: listen on 443 TCP/TLS for corporate networks; prefer TLS with certs.
- ONLYOFFICE: Document Server integration for Drive preview/edit (optional).
- tus protocol: resumable uploads (optional future).
