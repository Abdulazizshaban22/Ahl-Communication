nats:
  image: nats:2.10
config:
  cluster:
    enabled: true
  jetstream:
    enabled: true
  websocket:
    enabled: true
    port: 9222
    no_tls: true
resources:
  limits: { cpu: "2", memory: "4Gi" }
  requests: { cpu: "500m", memory: "1Gi" }
