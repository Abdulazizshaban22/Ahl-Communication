Label Studio setup:
- Start Label Studio and create a project "Ahla Emotion".
- Define labeling config for: sentiment (pos/neu/neg), emotions (GoEmotions), toxicity (multi-label).
- Enable pre-labels via webhook from low-confidence queue.
- Export approved labels to training/datasets/labeled.jsonl
