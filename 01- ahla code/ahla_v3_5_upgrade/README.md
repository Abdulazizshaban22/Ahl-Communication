# Ahla Suite — v3.5 Upgrade Packs
Tracks included:
1) `sagemaker/` — Pipelines + Model Monitor + Clarify + Model Registry + Feature Store.
2) `observability_security/` — Synthetics Canaries + X-Ray + WAF (Bot Control/ATP) + OpenSearch ingest.
3) `mobile/` — iOS (CallKit/PushKit + AppAuth + libsignal), Android (ConnectionService + FCM v1 + AppAuth + libsignal), Huawei (HMS Push Kit).
